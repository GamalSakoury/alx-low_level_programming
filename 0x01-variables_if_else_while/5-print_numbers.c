#include <stdio.h>
/**
 * main - A programming declaring a variable called n
 * to print the numbers from 0 to 9
 * by using a for loop and printf
 *
 * Return: 0 (success)
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}
	printf("\n");

	return (0);
}
