#include "main.h"

/**
  * mul - A function that multiplies two integers.
  * @a: the first passing parameter.
  * @b: the second passing parameter.
  * Return: a * b.
  */
int mul(int a, int b)
{
	return (a * b);
}
